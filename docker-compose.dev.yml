services:
  kitchen-server:
    build:
      context: ./kitchen_server
      target: development
    volumes:
      - ./kitchen_server/src:/app/src:ro
      - ./kitchen_server/tsconfig.json:/app/tsconfig.json:ro
    environment:
      - NODE_ENV=development
